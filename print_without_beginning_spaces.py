#ask the user to input a string with several spaces before the text itself
user_string = input("input a string with however much spaces you want before the text = ")

#print the user input without the spaces put before the text
print(user_string.lstrip())